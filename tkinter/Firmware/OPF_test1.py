from tkinter import messagebox
import datetime as dt

# today = dt.datetime.now().strftime("%H:%M")
# messagebox.showinfo(title="Now it is:", message=today)
messagebox.showerror(title="Error", message="No data yet.")
